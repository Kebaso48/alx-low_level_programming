#include <stdio.h>
/**
 * main - prints progrm name followed by new line
 * @argc: Number of argumens the program has
 * @argv: Array of pointers to arguments
 * Return: Always 0
 */
int main(int __attribute((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
